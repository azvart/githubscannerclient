import { gql } from "@apollo/client";export const GET_REPOSITORIES = gql`query repositories($withHooks: Boolean!){    repositories(withHooks:$withHooks) {        id        name        size        numberOfFiles        activeWebhooks {            id            name            active        }        owner        private    }}`export const GET_REPOSITORY = gql`    query repository($owner: String!, $repository: String!){        repository(owner:$owner, repository:$repository){            id            name            owner        }    }`